steps:
  check:
    image: denoland/deno:debian-2.9.5
    when:
      event: [push, pull_request, tag, manual]
    commands:
      - deno --version
      - deno install
      - deno task check
      - deno task test
      - deno task build
